///
/// HybridPdfViewComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridPdfViewComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridPdfViewSpecSwift.hpp"
#import "ReactNativeInkSignPdf-Swift-Cxx-Umbrella.hpp"

#if __has_include(<cxxreact/ReactNativeVersion.h>)
#include <cxxreact/ReactNativeVersion.h>
#if REACT_NATIVE_VERSION_MINOR >= 82
#define ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
#endif
#endif

using namespace facebook;
using namespace margelo::nitro::inksignpdf;
using namespace margelo::nitro::inksignpdf::views;

/**
 * Represents the React Native View holder for the Nitro "PdfView" HybridView.
 */
@interface HybridPdfViewComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridPdfViewComponent {
  std::shared_ptr<HybridPdfViewSpecSwift> _hybridView;
  BOOL _didDropView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridPdfViewComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridPdfViewComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    _props = HybridPdfViewShadowNode::defaultSharedProps();
    std::shared_ptr<HybridPdfViewSpec> hybridView = ReactNativeInkSignPdf::ReactNativeInkSignPdfAutolinking::createPdfView();
    _hybridView = std::dynamic_pointer_cast<HybridPdfViewSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  ReactNativeInkSignPdf::HybridPdfViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) notifyOnDropView {
  // A recycled component can later be invalidated. Notify only once per mount.
  if (_didDropView) {
    return;
  }
  ReactNativeInkSignPdf::HybridPdfViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.onDropView();
  _didDropView = YES;
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // A props update marks a newly mounted or still-active component.
  _didDropView = NO;

  // 1. Downcast props
  const auto& newViewProps = *std::static_pointer_cast<const HybridPdfViewProps>(props);
  const auto* oldViewProps = static_cast<const HybridPdfViewProps*>(oldProps.get());
  ReactNativeInkSignPdf::HybridPdfViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update only props that differ from the previous Props snapshot.
  const bool hasTransactionPropChanges = oldViewProps == nullptr
      ? newViewProps.hasAnyProvidedProps()
      : !newViewProps.hasSameProps(*oldViewProps);
  if (hasTransactionPropChanges) {
    swiftPart.beforeUpdate();

    // strokeColor: optional
    if (oldViewProps == nullptr
          ? newViewProps.strokeColor.isProvided()
          : !newViewProps.strokeColor.hasSameValue(oldViewProps->strokeColor)) {
      swiftPart.setStrokeColor(newViewProps.strokeColor.get());
    }
    // strokeMinWidth: optional
    if (oldViewProps == nullptr
          ? newViewProps.strokeMinWidth.isProvided()
          : !newViewProps.strokeMinWidth.hasSameValue(oldViewProps->strokeMinWidth)) {
      swiftPart.setStrokeMinWidth(newViewProps.strokeMinWidth.get());
    }
    // strokeMaxWidth: optional
    if (oldViewProps == nullptr
          ? newViewProps.strokeMaxWidth.isProvided()
          : !newViewProps.strokeMaxWidth.hasSameValue(oldViewProps->strokeMaxWidth)) {
      swiftPart.setStrokeMaxWidth(newViewProps.strokeMaxWidth.get());
    }
    // strokeSmoothing: optional
    if (oldViewProps == nullptr
          ? newViewProps.strokeSmoothing.isProvided()
          : !newViewProps.strokeSmoothing.hasSameValue(oldViewProps->strokeSmoothing)) {
      swiftPart.setStrokeSmoothing(newViewProps.strokeSmoothing.get());
    }
    // defaultTextFontSize: optional
    if (oldViewProps == nullptr
          ? newViewProps.defaultTextFontSize.isProvided()
          : !newViewProps.defaultTextFontSize.hasSameValue(oldViewProps->defaultTextFontSize)) {
      swiftPart.setDefaultTextFontSize(newViewProps.defaultTextFontSize.get());
    }
    // defaultTextColor: optional
    if (oldViewProps == nullptr
          ? newViewProps.defaultTextColor.isProvided()
          : !newViewProps.defaultTextColor.hasSameValue(oldViewProps->defaultTextColor)) {
      swiftPart.setDefaultTextColor(newViewProps.defaultTextColor.get());
    }
    // outlineColor: optional
    if (oldViewProps == nullptr
          ? newViewProps.outlineColor.isProvided()
          : !newViewProps.outlineColor.hasSameValue(oldViewProps->outlineColor)) {
      swiftPart.setOutlineColor(newViewProps.outlineColor.get());
    }
    // selectedOutlineColor: optional
    if (oldViewProps == nullptr
          ? newViewProps.selectedOutlineColor.isProvided()
          : !newViewProps.selectedOutlineColor.hasSameValue(oldViewProps->selectedOutlineColor)) {
      swiftPart.setSelectedOutlineColor(newViewProps.selectedOutlineColor.get());
    }
    // editorBackgroundColor: optional
    if (oldViewProps == nullptr
          ? newViewProps.editorBackgroundColor.isProvided()
          : !newViewProps.editorBackgroundColor.hasSameValue(oldViewProps->editorBackgroundColor)) {
      swiftPart.setEditorBackgroundColor(newViewProps.editorBackgroundColor.get());
    }
    // selectedBackgroundColor: optional
    if (oldViewProps == nullptr
          ? newViewProps.selectedBackgroundColor.isProvided()
          : !newViewProps.selectedBackgroundColor.hasSameValue(oldViewProps->selectedBackgroundColor)) {
      swiftPart.setSelectedBackgroundColor(newViewProps.selectedBackgroundColor.get());
    }
    // doubleTap: optional
    if (oldViewProps == nullptr
          ? newViewProps.doubleTap.isProvided()
          : !newViewProps.doubleTap.hasSameValue(oldViewProps->doubleTap)) {
      swiftPart.setDoubleTap(newViewProps.doubleTap.get());
    }
    // keyboardAvoidanceEnabled: optional
    if (oldViewProps == nullptr
          ? newViewProps.keyboardAvoidanceEnabled.isProvided()
          : !newViewProps.keyboardAvoidanceEnabled.hasSameValue(oldViewProps->keyboardAvoidanceEnabled)) {
      swiftPart.setKeyboardAvoidanceEnabled(newViewProps.keyboardAvoidanceEnabled.get());
    }
    // onStateChange: optional
    if (oldViewProps == nullptr
          ? newViewProps.onStateChange.isProvided()
          : !newViewProps.onStateChange.hasSameValue(oldViewProps->onStateChange)) {
      swiftPart.setOnStateChange(newViewProps.onStateChange.get());
    }
    // onPageChange: optional
    if (oldViewProps == nullptr
          ? newViewProps.onPageChange.isProvided()
          : !newViewProps.onPageChange.hasSameValue(oldViewProps->onPageChange)) {
      swiftPart.setOnPageChange(newViewProps.onPageChange.get());
    }

    // Update hybridRef if it changed
    if (oldViewProps == nullptr
          ? newViewProps.hybridRef.isProvided()
          : !newViewProps.hybridRef.hasSameValue(oldViewProps->hybridRef)) {
      // hybridRef changed - call it with new this
      const auto& maybeFunc = newViewProps.hybridRef.get();
      if (maybeFunc.has_value()) {
        maybeFunc.value()(_hybridView);
      }
    }

    swiftPart.afterUpdate();
  }

  // 3. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return ReactNativeInkSignPdf::ReactNativeInkSignPdfAutolinking::isPdfViewRecyclable();
}

- (void)prepareForRecycle {
  [self notifyOnDropView];
  [super prepareForRecycle];
  ReactNativeInkSignPdf::HybridPdfViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

#ifdef ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
- (void)invalidate {
  [self notifyOnDropView];
  [super invalidate];
}
#endif

@end
